export type IUpdateMetaOptions = {
    name?: string;
    parent?: string;
    description?: string;
};

export type ICreateFolderOptions = {
    name?: string;
    parent?: string;
    description?: string;
};
